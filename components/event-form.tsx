"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export function EventForm(){const router=useRouter();const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");async function submit(formData:FormData){setBusy(true);setMessage("");const response=await fetch("/api/events",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(formData.entries()))});const result=await response.json();if(!response.ok)setMessage(result.error||"Could not create event.");else{setMessage("Event created.");(document.getElementById("event-form")as HTMLFormElement)?.reset();router.refresh()}setBusy(false)}return <form id="event-form" className="card panel" action={submit}><div className="panel-head"><div><h2>Create event</h2><p>Creates a real database record. Registration and attendance attach to this event later.</p></div></div><div className="form-grid"><div className="field"><label>Event name</label><input name="title" required placeholder="e.g. Design Thinking Workshop"/></div><div className="field"><label>Venue</label><input name="venue" required placeholder="e.g. Innovation Lab"/></div><div className="field"><label>Start</label><input name="start_at" type="datetime-local" required/></div><div className="field"><label>End</label><input name="end_at" type="datetime-local"/></div><div className="field"><label>Capacity</label><input name="capacity" type="number" min="1" defaultValue="100" required/></div><div className="field"><label>Status</label><select name="status" defaultValue="draft"><option value="draft">Draft</option><option value="published">Published</option></select></div></div><div className="field" style={{marginTop:12}}><label>Description</label><textarea name="description" rows={3} placeholder="What is this event about?"/></div><div style={{display:"flex",justifyContent:"flex-end",gap:8,marginTop:15,alignItems:"center"}}>{message&&<span style={{fontSize:10,color:message.includes("created")?"#16845a":"#c23b3b"}}>{message}</span>}<button className="primary" disabled={busy}>{busy?"Creating…":"Create event"}</button></div></form>}