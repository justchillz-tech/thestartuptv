const pageNames={overview:"Overview",events:"Events",attendance:"Attendance",students:"Students",certificates:"Certificates",reports:"Reports","academic-attendance":"Academic Attendance",marks:"Marks",courses:"Courses",timetable:"Timetable",exams:"Exams",notices:"Notices",notifications:"Notifications",circulars:"Circulars",deadlines:"Deadlines",clubs:"Clubs","student-events":"Student Events",registrations:"Registrations","student-attendance":"Student Attendance",participation:"Participation","student-certificates":"Certificates","academic-record":"Academic Record","activity-record":"Activity Record",achievements:"Achievements",settings:"Settings"};
const pages=[...document.querySelectorAll(".page")],nav=[...document.querySelectorAll(".nav-item[data-page]")],title=document.getElementById("pageTitle"),sidebar=document.getElementById("sidebar"),modal=document.getElementById("modal"),modalCard=document.getElementById("modalCard");
function showPage(id){pages.forEach(p=>p.classList.toggle("active",p.id===id));nav.forEach(n=>n.classList.toggle("active",n.dataset.page===id));title.textContent=pageNames[id]||"Overview";window.scrollTo({top:0,behavior:"smooth"});sidebar.classList.remove("open");bindActions()}
function toast(msg){const old=document.querySelector(".toast");if(old)old.remove();const t=document.createElement("div");t.className="toast";t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.remove(),2600)}
function closeModal(){modal.classList.remove("open");modal.setAttribute("aria-hidden","true")}
function openEventModal(){modal.classList.add("open");modal.setAttribute("aria-hidden","false");modalCard.innerHTML='<button class="modal-close" data-close-modal>×</button><p class="eyebrow">NEW EVENT</p><h2>Create an event</h2><p>Registration, QR attendance and participation tracking are linked automatically.</p><label>Event name<input id="eventName" placeholder="e.g. Entrepreneurship 101"></label><div class="form-row"><label>Date<input id="eventDate" type="date"></label><label>Time<input id="eventTime" type="time"></label></div><label>Venue<input id="eventVenue" placeholder="e.g. Seminar Hall"></label><div class="form-actions"><button class="outline" data-close-modal>Cancel</button><button class="primary" id="saveEvent">Create event</button></div>';document.querySelectorAll("[data-close-modal]").forEach(x=>x.onclick=closeModal);document.getElementById("saveEvent").onclick=saveEvent;setTimeout(()=>document.getElementById("eventName").focus(),50)}
function saveEvent(){const name=document.getElementById("eventName").value.trim();if(!name){toast("Enter an event name first.");return}const item={name,date:document.getElementById("eventDate").value,time:document.getElementById("eventTime").value,venue:document.getElementById("eventVenue").value};const list=JSON.parse(localStorage.getItem("uloka-events")||"[]");list.push(item);localStorage.setItem("uloka-events",JSON.stringify(list));closeModal();toast("Event created. Registration and QR attendance are ready.");showPage("events")}
function bindActions(){document.querySelectorAll("[data-page]").forEach(x=>{if(!x.dataset.bound){x.dataset.bound="1";x.addEventListener("click",()=>showPage(x.dataset.page))}});document.querySelectorAll("[data-action]").forEach(x=>{x.onclick=()=>{const a=x.dataset.action;if(a==="create-event")openEventModal();else if(a==="scanner")toast("Scanner opened — camera permission would start here.");else if(a==="attendees")toast("Attendee list opened.");else if(a==="manage-event")toast("Event management opened.");else if(a==="issue-certificates")toast("Certificate batch queued for verified participants.");else if(a==="export")toast("Export prepared for download.");else if(a==="register")toast("Registration confirmed. Your participation record will update after check-in.");else if(a==="new-notice")toast("Notice composer opened.");else if(a==="mark-read")toast("All notifications marked as read.");else if(a==="open-document")toast("Document viewer opened.");else if(a==="find-club")toast("Club directory search opened.");else if(a==="club-view")toast("Club profile opened.");else if(a==="open-certificate")toast("Certificate opened.");else if(a==="edit-profile")toast("Profile editor opened.")}})}
document.getElementById("menuBtn").onclick=()=>sidebar.classList.toggle("open");document.querySelectorAll("[data-close-modal]").forEach(x=>x.onclick=closeModal);modal.onclick=e=>{if(e.target===modal||e.target.classList.contains("modal-backdrop"))closeModal()};document.addEventListener("keydown",e=>{if(e.key==="Escape")closeModal()});document.addEventListener("input",e=>{if(e.target.id==="studentSearch"){const q=e.target.value.toLowerCase();document.querySelectorAll("#studentTable tbody tr").forEach(r=>r.style.display=r.textContent.toLowerCase().includes(q)?"":"none")}});bindActions();showPage("overview");