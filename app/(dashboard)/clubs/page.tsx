import{getClubs}from"@/lib/data";export const dynamic="force-dynamic";export default async function ClubsPage(){const{configured,clubs}=await getClubs();return <><div className="page-heading"><div><p className="kicker">Student life / Clubs</p><h1>Clubs</h1><p className="subtle">Manage communities that own and run campus events.</p></div></div>{!configured&&<div className="setup">Connect Supabase to load clubs. No sample clubs are injected into the interface.</div>}{clubs.length===0?<div className="card empty"><strong>No clubs configured</strong><p>Create clubs after your institution is set up. Membership and event ownership will attach to this record.</p></div>:<div className="grid-3">{clubs.map((club:any)=><div className="card panel" key={club.id}><span className="metric-icon">✦</span><h2 style={{marginTop:12}}>{club.name}</h2><p className="subtle">{club.description||"No description"}</p><div style={{marginTop:15,fontSize:9,color:"#7b8492"}}>{club.status}</div></div>)}</div>}</>}