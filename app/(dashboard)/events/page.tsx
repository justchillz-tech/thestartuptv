import{getEvents}from"@/lib/data";import{EventForm}from"@/components/event-form";export const dynamic="force-dynamic";
export default async function EventsPage(){const{configured,events}=await getEvents();return <><div className="page-heading"><div><p className="kicker">Campus / Events</p><h1>Events</h1><p className="subtle">Create the source record for registrations, QR attendance and participation.</p></div></div>{!configured&&<div className="setup">Connect Supabase to activate persistence. This page intentionally does not fall back to hard-coded records.</div>}<div className="grid-2"><EventForm/><section className="card panel"><div className="panel-head"><div><h2>Event lifecycle</h2><p>The workflow this module will drive.</p></div></div>{["Draft → Published","Registration → Attendance","Attendance → Participation","Participation → Certificate","Event → Report"].map((x,i)=><div className="list-row" key={x}><strong style={{fontSize:10}}>{i+1}. {x}</strong></div>)}</section></div><section className="card" style={{marginTop:15}}><div className="panel"><div className="panel-head"><div><h2>Stored events</h2><p>{events.length} records returned from the backend.</p></div></div>{events.length===0?<div className="empty"><strong>No records returned</strong><p>Once the database is connected and an event is created, it will appear here.</p></div>:<div className="table-wrap"><table><thead><tr><th>Event</th><th>Start</th><th>Venue</th><th>Capacity</th><th>Status</th></tr></thead><tbody>{events.map((e:any)=><tr key={e.id}><td><strong>{e.title}</strong><br/><span style={{fontSize:9,color:"#8a919e"}}>{e.description||"No description"}</span></td><td>{new Date(e.start_at).toLocaleString("en-IN")}</td><td>{e.venue||"—"}</td><td>{e.capacity}</td><td>{e.status}</td></tr>)}</tbody></table></div>}</div></section></>}