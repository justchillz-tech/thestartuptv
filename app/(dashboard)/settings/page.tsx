"use client";import{useEffect,useState}from"react";export default function Settings(){const[data,setData]=useState<any>(null);useEffect(()=>{fetch("/api/me").then(r=>r.json()).then(setData)},[]);return <><div className="page-heading"><div><p className="kicker">Administration / Settings</p><h1>Settings</h1><p className="subtle">Institution identity, account role and deployment readiness.</p></div></div><div className="grid-2"><section className="card panel"><h2>Current account</h2><div className="list-row"><strong>Email</strong><span style={{float:"right",fontSize:10}}>{data?.user?.email||"Not connected"}</span></div><div className="list-row"><strong>Role</strong><span style={{float:"right",fontSize:10}}>{data?.profile?.role||"—"}</span></div><div className="list-row"><strong>Institution</strong><span style={{float:"right",fontSize:10}}>{data?.profile?.institution_id||"Not linked"}</span></div></section><section className="card panel"><h2>Connection policy</h2><p className="subtle">No production connection is stored in this repository. Add environment variables only when you are ready to connect the verified build.</p><div className="setup" style={{marginTop:16}}>Vercel deployment is intentionally not part of this build.</div></section></div></>}