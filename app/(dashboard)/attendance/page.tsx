export default function AttendancePage(){return <><div className="page-heading"><div><p className="kicker">Campus / Attendance</p><h1>Attendance</h1><p className="subtle">QR attendance verifies event participation and feeds the student activity record.</p></div></div><div className="grid-3"><div className="card panel"><h2>Event check-in</h2><p className="subtle">Generate a short-lived QR session from an active event.</p><div className="empty" style={{marginTop:18}}><strong>QR scanner service</strong><p>Next build step: signed QR tokens, expiry, duplicate protection and attendance sessions.</p></div></div><div className="card panel"><h2>Verification</h2><p className="subtle">Every check-in is tied to a student and event.</p><div className="list-row">Registered student → verified</div><div className="list-row">Valid session → verified</div><div className="list-row">Duplicate scan → rejected</div></div><div className="card panel"><h2>Audit trail</h2><p className="subtle">Attendance actions will be recorded for institutional reporting.</p><div className="empty"><strong>No sessions yet</strong><p>Sessions appear once QR attendance is enabled.</p></div></div></div></>}