"use client";import{useEffect,useState}from"react";export default function Reports(){const[data,setData]=useState<Record<string,number>|null>(null);const[error,setError]=useState("");useEffect(()=>{fetch("/api/reports").then(r=>r.json()).then(j=>{if(j.error)setError(j.error);else setData(j.counts)})},[]);return <><div className="page-heading"><div><p className="kicker">Administration / Reports</p><h1>Reports</h1><p className="subtle">Live operational counts from the institution database.</p></div></div>{error&&<div className="setup">{error}</div>}<div className="grid-4">{["students","clubs","events","registrations","attendance","certificates","courses","marks"].map(k=><div className="card metric" key={k}><div className="metric-top"><span>{k.replaceAll("_"," ")}</span><span className="metric-icon">▥</span></div><span className="metric-value">{data?data[k]??0:"—"}</span><span className="metric-foot">Live count</span></div>)}</div></>}