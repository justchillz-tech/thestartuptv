import{getStudents}from"@/lib/data";export const dynamic="force-dynamic";export default async function StudentsPage(){const{configured,students}=await getStudents();return <><div className="page-heading"><div><p className="kicker">Campus / Students</p><h1>Students</h1><p className="subtle">The institutional student record becomes the source for academics and student life.</p></div></div>{!configured&&<div className="setup">Connect Supabase to load student records. No placeholder student data is displayed.</div>}<section className="card"><div className="panel"><div className="panel-head"><div><h2>Student directory</h2><p>{students.length} records returned.</p></div></div>{students.length===0?<div className="empty"><strong>No student records yet</strong><p>Run the schema, create an institution and add students. The directory will read directly from PostgreSQL.</p></div>:<div className="table-wrap"><table><thead><tr><th>Student</th><th>Student ID</th><th>Email</th><th>Year</th><th>Status</th></tr></thead><tbody>{students.map((s:any)=><tr key={s.id}><td><strong>{s.first_name} {s.last_name}</strong></td><td>{s.student_number}</td><td>{s.email}</td><td>{s.year_of_study}</td><td>{s.status}</td></tr>)}</tbody></table></div>}</div></section></>}